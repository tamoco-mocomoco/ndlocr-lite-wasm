services:
  dev:
    build: .
    working_dir: /app
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "6174:6174"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
volumes:
  node_modules:
